require("@nomicfoundation/hardhat-toolbox");

module.exports = {
  solidity: "0.8.20",
  networks: {
    polygon: {
      url: "https://polygon-rpc.com", // Replace with your Polygon RPC endpoint
      accounts: ["YOUR_PRIVATE_KEY"] // Replace with your wallet private key
    }
  }
};require("@nomicfoundation/hardhat-toolbox");

module.exports = {
  solidity: "0.8.20",
  networks: {
    polygon: {
      url: "https://polygon-rpc.com", // Replace with your Polygon RPC endpoint
      accounts: ["YOUR_PRIVATE_KEY"] // Replace with your wallet private key
    }
  }
};
